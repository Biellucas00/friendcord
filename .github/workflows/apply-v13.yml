name: Apply FriendCord v13
on:
  push:
    branches: [main]
    paths:
      - FriendCord-v13-admin-search.zip
      - .github/workflows/apply-v13.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: |
          unzip -o FriendCord-v13-admin-search.zip
          rm FriendCord-v13-admin-search.zip
          rm .github/workflows/apply-v13.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'Aplicar busca de administradores globais'
          git push
