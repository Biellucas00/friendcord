name: Apply FriendCord v12
on:
  push:
    branches: [main]
    paths:
      - FriendCord-v12-mobile.zip
      - .github/workflows/apply-v12.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: |
          unzip -o FriendCord-v12-mobile.zip
          rm FriendCord-v12-mobile.zip
          rm .github/workflows/apply-v12.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'Aplicar interface mobile responsiva'
          git push
